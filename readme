steps
1. to use bootstrap better have a div with a class container or container-fluid
2. link your bootstrap : use cdn or local package : also add a meta for the viewport
3. build out the grid. 
   rules : columns are within containers 
4. add elements inside the div with class col.
5. style using either or bootstrap classes or local css 